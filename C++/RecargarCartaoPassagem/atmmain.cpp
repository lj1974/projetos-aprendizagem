#include <iostream>
#include <cstdio>
#include <string>
using namespace std;

#define limpar system("clear");
#define setbuf setbuf(stdin, NULL);

#include "setup.h"

int main() {
  Atm show;
  show.setScreen();

  return 0;
}